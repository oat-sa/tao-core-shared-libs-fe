# fix: update bundled ckeditor for taofurigana cursor handling

## Summary

- updates bundled `lib/ckeditor/ckeditor.js` with TaoFurigana plugin fix from `ckeditor-dev`
- ships runtime behavior where cursor enters `rt` correctly without adding `&nbsp;` placeholders

## Why

TAO runtime consumes CKEditor from `@oat-sa/tao-core-shared-libs`. This bundle update is required for local and CI environments to use the plugin fix.

## Scope

- `lib/ckeditor/ckeditor.js`

## Validation

- yalc-linked TAO instance shows working furigana cursor behavior
- no `appendHtml('&nbsp;')` insertion path remains in `taofurigana` runtime chunk

## Related

- Jira: INF-466
- Dependent: INF-283
